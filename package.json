{
  "name": "projeto-completo",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd src/backend/Truckcontrol.api && dotnet watch run",
    "dev:frontend": "cd src/frontend/truck-control && ng serve",
    "build:backend": "cd backend/Truckcontrol.api && dotnet build",
    "build:frontend": "cd frontend && ng build",
    "install:all": "cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}